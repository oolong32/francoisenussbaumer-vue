{% extends "_layout" %}

{% block content %}
  {# 7.9.22: auction out of order, hier braucht es ein schönes bild
  {% set testimageUrl = alias( '@assetBasePath' ) ~ "/_pic/test.svg" %} 
  #}

{# kann gelöscht rückgängig gemacht werden, wenn auction wieder läuft #}
  {% set testimageUrl = alias( '@assetBasePath' ) ~ "/_pic/placeholder.jpg" %} 

  <main-teaser :testimageurl="{{ testimageUrl|json_encode }}"></main-teaser>

{% endblock %}
